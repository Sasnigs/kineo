import { build } from 'esbuild';
import { fileURLToPath } from 'node:url';

// Compile the same tested domain source for the Edge sandbox, which mounts only
// supabase/functions. Generated output is ignored and rebuilt before serving.
await build({
  absWorkingDir: fileURLToPath(new URL('..', import.meta.url)),
  entryPoints: ['src/core/account/server-plan-command.ts'],
  outfile: fileURLToPath(new URL('../../../supabase/functions/_shared/generated/authoritative-plan.js', import.meta.url)),
  bundle: true,
  platform: 'neutral',
  format: 'esm',
  target: 'es2022',
  banner: { js: '// Generated by npm run account:bundle. Edit the shared TypeScript source, not this file.' },
  logLevel: 'warning',
});
